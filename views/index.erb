<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <!-- The above 3 meta tags *must* come first in the head; any other head content must come *after* these tags -->
   <script src="https://ajax.googleapis.com/ajax/libs/jquery/2.1.4/jquery.min.js"></script>
    <!--<script src="ajax.js"></script>-->
    <title>prefix tree</title>
    <script type="text/javascript">
      $(document).ready(function (){
        $('#get-list').on('click', function(event){
          event.preventDefault();

          $.ajax({
            url: '/list',
            type: 'GET',
            success: function(resp){
              console.log(resp);
              $('#list').append(resp);
            }
          })
        })

        $('#add-form').on('submit', function(event){
          
          $.ajax({
            url: '/add',
            type: 'GET',
            data: $("#add-form").serialize(),
            success: function(resp){
              console.log(resp);
            }
          })
          event.preventDefault();
        })
      });
    </script>
  </head>
  <body>

  <form id="add-form" action="/add" method="GET">
    <input type="text" name="word">
    <input id="add" type="submit" value="add" class="btn">
  </form>
  <div id="list"></div>
  <input id="get-list" type="submit">
  <!--
  <form id="list-form" >
    <input type="text" name="prefix">
    <input id="get-list" type="submit" value="list" class="btn">
  </form>
  -->
<!--
  <form action="/list" method="post">
    <input type="submit" value="list" class="btn btn-primary">
  </form>
-->
</body>